//ENUM

enum CustomSize {
    SMALL = 46,
    MEDIUM = 50,
    LARGE = 55,
    EXTRA_LARGE = 60,
    STATUS,
}

enum CustomChange {
    EASY,
    MEDIUM,
    HARD
}

const size = CustomSize.SMALL
console.log(size);

